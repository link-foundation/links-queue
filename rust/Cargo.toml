[package]
name = "links-queue"
version = "0.3.0"
edition = "2021"
description = "A Rust implementation of Links Queue - universal queue system using links"
readme = "README.md"
license = "Unlicense"
keywords = ["links-queue", "queue", "links", "rust"]
categories = ["development-tools"]
repository = "https://github.com/link-foundation/links-queue"
documentation = "https://github.com/link-foundation/links-queue"
rust-version = "1.70"

[lib]
name = "links_queue"
path = "src/lib.rs"

[[bin]]
name = "links-queue"
path = "src/main.rs"

[dependencies]
tokio = { version = "1.0", features = ["rt-multi-thread", "macros", "time", "process", "io-util", "sync", "net", "signal"] }

# Optional web framework integrations
axum = { version = "0.7", optional = true }
actix-web = { version = "4", optional = true }
serde = { version = "1.0", features = ["derive"], optional = true }
serde_json = { version = "1.0", optional = true }
tower = { version = "0.5", optional = true }
tower-service = { version = "0.3", optional = true }
http = { version = "1.0", optional = true }
http-body-util = { version = "0.1", optional = true }

[features]
default = []
axum = ["dep:axum", "dep:serde", "dep:serde_json", "dep:tower", "dep:tower-service", "dep:http", "dep:http-body-util"]
actix = ["dep:actix-web", "dep:serde", "dep:serde_json"]
full = ["axum", "actix"]

[dev-dependencies]
tokio-test = "0.4"
tempfile = "3"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
nursery = { level = "warn", priority = -1 }

# Allow some common patterns
module_name_repetitions = "allow"
too_many_lines = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"

[profile.release]
lto = true
codegen-units = 1
strip = true
